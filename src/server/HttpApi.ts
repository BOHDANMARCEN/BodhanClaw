// Minimal stub for future HTTP API.
// ASSUMPTION: HTTP server is deferred until Phase 4.

export class HttpApi {
  start(port = 4153): void {
    console.log(`HTTP API stub listening on http://localhost:${port} (not implemented)`);
  }
}
